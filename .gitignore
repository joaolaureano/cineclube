# dependencias e artefatos de build
node_modules/
dist/
build/
coverage/

# pacote da Lambda: gerado por backend/build-lambda.sh a cada deploy, ~32 MB
dist-lambda/
dist-lambda.zip

# ambiente
.env
.env.local
*.local

# infraestrutura: o  e mantido fora do repositorio de proposito.
# . guarda segredos em texto plano (a connection string) e os JSONs
# de policy descrevem a superficie IAM da conta. Nada disso entra num repo
# publico.
/
backend//
*.
*..*
*.
!*..example
./
..lock.hcl

# ferramentas locais
graphify-out/
.serena/

# SO / editor
.DS_Store
*.swp
.idea/
.vscode/
