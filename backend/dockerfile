FROM node:18-alpine AS base

WORKDIR /app

COPY package*.json /app/
RUN npm ci

COPY . .

# tsoa gera as rotas e o swagger; ambos são pré-requisitos do tsc
RUN npm run tsoa:gen && npx tsc

EXPOSE 5000

CMD ["node", "dist/src/index.js"]
